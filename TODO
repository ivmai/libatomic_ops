== TODO tasks ==

Move all primitives based on __sync intrinsic from gcc/x86.h to gcc/generic.h;
offer more sync-base alternatives if AO_USE_SYNC_BUILTINS defined.

Add C++0x ATM (atomic memory operations) layer.


== FIXME tasks ==

Debian/powerpc (gcc 4.6.3-7):
test_stack failed (Debian Bug #680100).

Debian/alpha (gcc 4.6.3-7):
test_stack failed (Debian Bug #680100) caused by a compiler bug while
handling __builtin_expect in atomic_ops_stack.c.

Debian/m68k (Linux 3.2.0-2-atari):
test_stack failed (Bus error), regression (Debian Bug #680066).
